import React from 'react';
import ListScheduleClient from './ListScheduleClient';
import { getAllStudents, ScheduleItem } from "../../../lib/data";

export default async function ListSchedule() {

    return (
        <ListScheduleClient
        />
    );
}